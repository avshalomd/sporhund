# Copy this file to .env and fill in your own values.
# .env is git-ignored; .env.example is committed and must never contain secrets.

# Statens vegvesen vehicle-data API key (optional).
# Order your own — keys are personal and must not be shared or bundled:
#   https://www.vegvesen.no/kjoretoy/eie/kjoretoyopplysninger/bestill-api-nokkel/
VEGVESEN_API_KEY=
