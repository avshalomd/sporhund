---
description: Render the listings we're discussing as a visual page — a thumbnail grid, or a full dossier for one listing.
---

Use the `listing-view` skill to render what we are currently looking at:

- If we are discussing a search, render the results grid with the same filters
  you last searched with.
- If we are discussing one listing, render the full dossier, with
  `--comparables`, and with `--verify` when it is a car and a Statens vegvesen
  key is configured.

Publish it as an artifact, then tell me which ones are worth my attention and
why. Keep the artifact private — it contains the sellers' photographs.

$ARGUMENTS
